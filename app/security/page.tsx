import { ContactShell } from "@/components/credit-vivo/ContactShell";

export default function Page() {
  return (
    <ContactShell
      title="Security Contact"
      subtitle="Report suspicious account activity, security concerns, or domain/email abuse. Credit reports, IDs, scanner outputs, workbooks, and customer messages must stay in approved secure systems."
      primaryEmail="security"
    />
  );
}
