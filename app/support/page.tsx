import { ContactShell } from "@/components/credit-vivo/ContactShell";

export default function Page() {
  return (
    <ContactShell
      title="Support"
      subtitle="Get help with your account, portal access, document status, or next steps."
      primaryEmail="support"
    />
  );
}

